<header class="bg-stone-600 text-white p-4 fixed top-0 left-0 right-0 z-10">
  <div class="container mx-auto flex justify-between items-center">
    <div class="flex items-baseline">
      <a href="/">
        <h1 class="text-2xl font-bold">Bibliometria Arqueológica</h1>
      </a>
      <a href="/scientific_journals" class="ml-4">Revistas cientificas</a>
    </div>

    <div>
      <% if user_signed_in? %>
        <p class="text-sm mb-2">Logged in as <%= current_user.email %></p>
      <% end %>
      <div class="flex space-x-4">
        <% if user_signed_in? %>
          <%= link_to 'Edit Profile', edit_user_registration_path, class: "text-gray-300 hover:text-white" %>
          <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-300 hover:text-white" %>
          <%= link_to 'Delete Account', user_registration_path, method: :delete, data: { confirm: "Are you sure?" }, class: "text-red-500 hover:text-red-700" %>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: "text-gray-300 hover:text-white" %>
          <%= link_to 'Register', new_user_registration_path, class: "text-gray-300 hover:text-white" %>
        <% end %>
      </div>
    </div>
  </div>
</header>
